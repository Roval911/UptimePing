version: v1
# ВАЖНО: закомментируйте name пока не настроен доступ
# name: buf.build/slavarodionov/UptimePingPlatform

deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway

lint:
  use:
    - DEFAULT
  ignore:
    - proto/ignore

breaking:
  use:
    - WIRE_JSON
  ignore:
    - proto/ignore
