server:
  host: "0.0.0.0"
  port: 50058

database:
  host: "postgres"
  port: 5432
  name: "uptimeping"
  user: "uptimeping"
  password: "uptimeping123"

redis:
  addr: "redis:6379"
  password: ""
  db: 0

logger:
  level: "info"
  format: "json"

environment: "dev"

# CLI Configuration
api:
  base_url: "http://localhost:8080"
  timeout: 30

grpc:
  scheduler_address: "scheduler-service:50052"
  core_address: "core-service:50054"
  auth_address: "auth-service:50051"
  use_grpc: false
  timeout: 30

auth:
  token_expiry: 3600
  refresh_threshold: 300
  access_secret: "your-access-secret-key"
  refresh_secret: "your-refresh-secret-key"

output:
  format: "table"
  colors: true
